#include<iostream>
#include<vector>
using namespace std;

int main()
{
    vector <int>nums=[1,2,2,4];
    vector<bool>present(n+1,false);
    int n=nums.size()+1;
    for(i=0;i<n;i++)
    {
        if(nums[i])
    }
}